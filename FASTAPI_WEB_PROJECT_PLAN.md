# FastAPI 多租户工单协作平台：项目规划

## 1. 文档用途

本文档定义一个独立的 Python Web 后端项目。它与 RAG 智能体项目共享 Python 工程经验，但不依赖大模型，重点训练 HTTP、数据库、认证授权、并发、缓存、异步任务、测试和部署。

推进方式不按周或日期划分，只按模块依赖顺序推进。每个阶段由 Codex 提供设计和代码，学习者负责理解、运行、复刻关键实现并通过验收。

## 2. 项目定位

项目名称：多租户售后工单与团队协作平台

业务场景：多个企业租户使用同一套系统管理成员、客户、工单、评论、附件和通知。不同租户数据严格隔离，成员依据角色拥有不同权限，工单具有明确的状态流转、操作记录和异步通知。

选择该题目的原因：

- 业务结构足够真实，可以自然引出认证、RBAC、事务、索引和并发控制。
- 写操作较多，能够体现后端工程能力，而不只是 CRUD。
- 后续可以作为 RAG 智能体的真实工具服务，但两个项目可以独立运行和展示。

核心用户：

- 平台管理员：管理系统级配置和租户。
- 租户管理员：管理本企业成员、角色和业务配置。
- 客服成员：创建、领取、处理和转派工单。
- 普通用户：提交工单、补充信息和查看处理进度。

## 3. 默认技术方案

| 分类 | 默认选择 | 选择理由 |
|---|---|---|
| Python | Python 3.13 | 稳定并支持现代类型语法 |
| Web 框架 | FastAPI | 类型驱动、异步支持和自动 OpenAPI |
| 数据校验 | Pydantic v2 | 请求、响应、配置和领域数据校验 |
| ORM | SQLAlchemy 2.0 Async | 掌握异步 Session、事务和查询构造 |
| 数据库 | PostgreSQL | 支持可靠事务、索引、锁和生产常见特性 |
| 数据迁移 | Alembic | 版本化管理数据库结构 |
| 缓存/基础设施 | Redis | 缓存、限流、幂等和任务队列支撑 |
| 后台任务 | Celery + Redis | 处理通知、导出和耗时任务 |
| 认证 | JWT Access + Refresh Token | 学习完整登录、续期和撤销流程 |
| 密码 | Argon2/Bcrypt | 安全哈希，不保存明文密码 |
| 文件存储 | 本地适配器，增强阶段接 MinIO | 先降低启动成本，再学习对象存储 |
| 测试 | pytest + httpx | 单元、数据库集成和 API 测试 |
| 质量工具 | Ruff + mypy | 格式、静态检查和类型约束 |
| 部署 | Docker Compose | 统一应用、PostgreSQL、Redis 和 Worker 环境 |

## 4. 系统边界与主要实体

核心实体：

```text
Tenant 租户
  ├── User 用户
  ├── Membership 成员关系
  ├── Role / Permission 角色权限
  ├── Customer 客户
  └── Ticket 工单
        ├── Comment 评论
        ├── Attachment 附件
        ├── TicketEvent 操作记录
        └── Notification 通知
```

关键业务规则：

- 用户可以加入多个租户，但每次请求必须明确当前租户。
- 所有业务查询都必须包含租户隔离条件。
- 工单状态不能任意修改，必须通过状态机规定的动作流转。
- 修改、转派、关闭等操作需要对应权限。
- 创建和重复提交等关键接口需要幂等保护。
- 重要写操作产生不可随意覆盖的审计事件。

## 5. 计划目录结构

```text
fastapi-ticketing-system/
├── src/
│   └── ticketing/
│       ├── main.py
│       ├── api/
│       │   ├── dependencies.py
│       │   └── v1/              # 路由层
│       ├── core/                 # 配置、安全、日志、异常
│       ├── db/                   # Engine、Session、Base、迁移辅助
│       ├── models/               # SQLAlchemy 模型
│       ├── schemas/              # Pydantic 请求与响应模型
│       ├── repositories/         # 数据访问
│       ├── services/             # 业务规则和事务边界
│       ├── permissions/          # RBAC 与数据权限
│       ├── tasks/                # Celery 任务
│       ├── cache/                # Redis、限流、幂等
│       └── observability/        # 日志、指标、请求追踪
├── alembic/
├── tests/
│   ├── unit/
│   ├── integration/
│   └── api/
├── scripts/
├── .env.example
├── alembic.ini
├── pyproject.toml
├── Dockerfile
├── compose.yml
└── README.md
```

依赖方向：

```text
Router -> Service -> Repository -> Database
              |          |
              |          -> ORM Model
              -> Domain Rule / Permission

Schema 用于 API 边界，不直接承担复杂业务逻辑。
```

## 6. 实现阶段与依赖顺序

### 阶段 0：业务规则、API 草图与数据建模

实现内容：

- 写出四类用户故事和权限矩阵。
- 设计工单生命周期及允许的状态转换。
- 绘制 ER 图，确定主键、外键、唯一约束和删除策略。
- 草拟 API 路径、请求、响应和错误码。
- 明确同步操作与异步任务的边界。

交付物：需求文档、ER 图、权限矩阵、状态机和 API 草案。

验收条件：能够解释每个实体为什么存在，以及一个工单从创建到关闭发生了哪些数据变化。

### 阶段 1：工程骨架与应用生命周期

实现内容：

- 创建 `src` 布局、Git、`pyproject.toml` 和分层目录。
- 初始化 FastAPI App Factory、版本化路由、Settings 和日志。
- 实现 `/health/live` 与 `/health/ready`。
- 配置 Ruff、mypy、pytest、`.env.example` 和 `.gitignore`。
- 使用 lifespan 管理应用启动和关闭资源。

核心知识：ASGI、Uvicorn、FastAPI 生命周期、路由注册和配置加载。

验收条件：应用可启动；健康接口有明确响应模型；静态检查和测试通过。

### 阶段 2：数据库、异步 Session 与迁移

实现内容：

- 使用 SQLAlchemy 2.0 创建异步 Engine 和 Session Factory。
- 通过依赖注入为每次请求提供 Session。
- 创建 Declarative Base、时间字段和通用主键约定。
- 初始化 Alembic，完成第一版迁移与回滚。
- Docker Compose 启动 PostgreSQL。

核心知识：连接池、Session 生命周期、事务、ORM 与迁移的职责区别。

验收条件：迁移可从空库升级到最新版本并完整回滚；连接在请求结束后正确释放。

### 阶段 3：用户、密码与基础认证

实现内容：

- 用户注册、登录、查看个人资料和修改密码。
- 密码安全哈希与验证。
- 签发短期 Access Token 和长期 Refresh Token。
- Refresh Token 保存哈希并支持轮换、注销和撤销。
- 登录错误使用统一响应，避免泄露账号是否存在。

核心知识：认证与授权的区别、JWT 结构、密码哈希、Token 轮换与重放风险。

验收条件：密码从不明文存储；过期、篡改、撤销和重复使用 Token 均有测试。

### 阶段 4：租户、成员关系与数据隔离

实现内容：

- 创建租户、邀请成员、加入和退出租户。
- 用户通过 Header 或路径选择当前租户。
- 统一实现 `CurrentUser` 和 `CurrentTenant` 依赖。
- Repository 查询强制带租户条件。
- 添加跨租户访问测试。

核心知识：多租户模型、行级隔离、复合唯一约束、越权访问。

验收条件：知道其他租户资源 ID 也无法读取或修改；测试覆盖水平越权。

### 阶段 5：RBAC 角色权限

实现内容：

- 建立角色、权限、成员角色关系。
- 实现租户管理员、客服、普通用户的默认角色。
- 使用 FastAPI Dependency 检查操作权限。
- 区分功能权限和数据范围权限。
- 增加垂直越权和权限变更后的测试。

核心知识：RBAC、最小权限、401 与 403、依赖注入。

验收条件：权限矩阵中的每个允许与拒绝场景都有测试证据。

### 阶段 6：客户与工单核心 CRUD

实现内容：

- 客户创建、查询和更新。
- 工单创建、详情、列表和有限字段修改。
- 分离 Router、Service 和 Repository。
- Service 定义事务边界，Repository 只负责数据访问。
- 输出 DTO，禁止直接返回内部 ORM 对象结构。

核心知识：分层架构、DTO、事务边界、依赖倒置和 N+1 查询问题。

验收条件：核心 CRUD 完整；请求校验、资源不存在和唯一冲突返回正确状态码。

### 阶段 7：工单状态机、转派与审计日志

实现内容：

- 定义 `open -> in_progress -> resolved -> closed` 等合法状态。
- 使用动作方法进行领取、转派、解决、重开和关闭。
- 每次状态变更写入 `TicketEvent`。
- 在同一事务中同时保存业务变化和审计事件。
- 禁止通过普通 PATCH 绕过状态机。

核心知识：状态机、不变量、原子性、审计日志与普通应用日志的区别。

验收条件：所有非法状态跳转被拒绝；合法跳转均产生正确审计记录。

### 阶段 8：列表查询、分页、筛选与数据库索引

实现内容：

- 实现游标或稳定偏移分页。
- 支持状态、负责人、优先级、创建时间和关键词筛选。
- 设计稳定排序，避免翻页重复或漏数据。
- 使用 `EXPLAIN ANALYZE` 检查查询计划。
- 为真实查询模式添加单列或复合索引。

核心知识：分页策略、B-Tree、选择性、覆盖索引、查询计划。

验收条件：分页结果稳定；索引优化有前后查询计划证据，而不是只凭经验添加。

### 阶段 9：并发控制、幂等与事务冲突

实现内容：

- 防止两个客服同时领取同一工单。
- 比较行锁、乐观锁和条件更新，并选择一种主方案。
- 为创建工单等接口支持 `Idempotency-Key`。
- 定义唯一冲突、版本冲突和状态冲突的 409 响应。
- 编写并发测试复现竞争条件。

核心知识：隔离级别、丢失更新、行锁、乐观锁、幂等性。

验收条件：并发领取只有一个请求成功；重复请求不会创建重复资源。

### 阶段 10：评论、附件与文件安全

实现内容：

- 添加公开回复和内部备注。
- 实现附件上传、下载、大小限制和扩展名/MIME 校验。
- 文件存储通过接口隔离，默认本地实现，增强阶段接入 MinIO。
- 下载前检查租户和工单权限。
- 使用随机对象键，避免路径穿越和文件覆盖。

核心知识：Multipart、流式文件、对象存储、路径穿越、内容类型不可信。

验收条件：越权下载、超大文件和危险文件名均被正确处理。

### 阶段 11：Redis 缓存、限流与一致性

实现内容：

- 缓存低频变化的租户配置或权限快照。
- 对登录、验证码或高成本查询增加限流。
- 实现缓存键规范、TTL、失效和降级。
- 写操作成功后删除或更新相关缓存。
- Redis 不可用时核心业务仍能按策略运行。

核心知识：Cache Aside、缓存一致性、穿透、击穿、雪崩和限流算法。

验收条件：缓存命中与失效可观测；不会因缓存返回其他租户数据；Redis 故障有测试。

### 阶段 12：后台任务与可靠通知

实现内容：

- Celery Worker 处理邮件通知、批量导出等耗时操作。
- API 只负责提交任务并返回任务标识。
- 任务支持重试、退避、超时和幂等。
- 通过 Outbox 或等价方案降低“数据库提交成功但任务消息丢失”的风险。
- 提供任务状态查询和失败记录。

核心知识：消息队列、至少一次投递、重复消费、Outbox、最终一致性。

验收条件：重复执行不会发送不受控的重复通知；Worker 重启后任务可恢复或明确失败。

### 阶段 13：统一异常、日志、追踪与指标

实现内容：

- 设计领域异常和统一 HTTP 错误格式。
- 为请求生成 Correlation ID，并贯穿 API 与后台任务。
- 结构化记录请求耗时、状态码和错误类型。
- 脱敏 Token、密码、邮箱、手机号和附件信息。
- 统计接口延迟、错误率、数据库耗时和任务成功率。

核心知识：可观测性、应用日志与审计日志、错误边界、敏感信息处理。

验收条件：一次请求可以通过 ID 定位完整调用链；日志不泄露密钥和明文凭据。

### 阶段 14：测试体系与 API 契约

实现内容：

- 单元测试覆盖状态机、权限和纯业务规则。
- 集成测试使用独立 PostgreSQL 和 Redis。
- API 测试覆盖认证、租户隔离、分页、并发和异常。
- 使用 Fixture、Factory 和事务回滚隔离测试数据。
- 检查 OpenAPI Schema 和关键响应契约。

核心知识：测试金字塔、Mock 边界、Fixture 生命周期、确定性测试。

验收条件：核心业务测试稳定且可重复；测试不会依赖执行顺序或共享脏数据。

### 阶段 15：Docker、CI 与生产启动方式

实现内容：

- 编写多阶段 Dockerfile 和非 root 运行用户。
- Compose 编排 API、PostgreSQL、Redis 和 Celery Worker。
- 区分开发与生产配置。
- CI 执行静态检查、类型检查、测试和迁移检查。
- 使用正式启动参数，关闭 `--reload`，配置合理 Worker 数量和健康检查。

核心知识：容器网络、环境变量、进程模型、CI、迁移发布顺序。

验收条件：干净环境一条命令启动；CI 全部通过；服务重启不丢失数据库数据。

### 阶段 16：性能验证、文档与最终交付

实现内容：

- 为核心查询和写接口执行基础压力测试。
- 记录 P50/P95 延迟、吞吐量、错误率和测试环境。
- 编写 README、部署说明、ER 图、状态机、API 示例和故障排查指南。
- 准备可重复的演示数据和演示脚本。
- 整理简历描述、STAR 故事和面试题库。

验收条件：任何简历指标都能复现；能够从 HTTP 请求入口讲到数据库提交和响应返回的完整生命周期。

## 7. HTTP 与业务错误约定

项目实施时统一遵守：

| 状态码 | 使用场景 |
|---|---|
| 200/201/204 | 查询成功、创建成功、无正文成功 |
| 400 | 请求语义错误但不属于字段格式校验 |
| 401 | 未认证、Token 无效或过期 |
| 403 | 已认证但权限不足 |
| 404 | 当前租户范围内资源不存在 |
| 409 | 唯一冲突、版本冲突、状态冲突或幂等冲突 |
| 422 | 请求字段校验失败 |
| 429 | 超出限流 |
| 500/503 | 未处理错误或依赖服务暂时不可用 |

错误响应包含稳定错误码、可读消息和 Correlation ID，不向客户端返回内部堆栈。

## 8. 阶段完成模板

```text
阶段名称：
新增或修改的 API：
数据库变化：
核心业务规则：
运行与迁移命令：
测试结果：
安全检查：
性能或查询计划证据：
遇到的问题与根因：
最终设计取舍：
仍存在的限制：
我能独立解释的知识点：
```

## 9. 最终简历证据

最终可以使用的量化数据必须由测试产生：

- API 数量、核心实体数量和自动化测试数量。
- 测试通过率及关键业务分支覆盖情况。
- P50/P95 延迟、吞吐量和错误率，并注明测试环境。
- 索引优化前后的执行计划或耗时对比。
- 并发和幂等测试结果。
- 后台任务成功率、重试和故障恢复结果。

禁止在没有验证时写“支持高并发”“生产级”“零故障”或虚构用户规模。

## 10. 默认简历表述草案

- 基于 FastAPI、SQLAlchemy 2.0 Async 与 PostgreSQL 设计多租户工单协作平台，通过租户上下文和 RBAC 实现成员权限与数据隔离。
- 使用领域 Service、Repository 和状态机组织工单领取、转派、解决、重开等业务流程，并通过事务与审计事件维护业务一致性。
- 针对重复提交和并发领取实现幂等键与并发控制；结合 Redis 完成缓存、限流和故障降级。
- 使用 Celery 处理通知与导出任务，设计重试、任务幂等和可靠消息机制；通过 pytest、Docker Compose 和 CI 完成可复现交付。

## 11. 与 RAG 智能体项目的可选连接

两个项目完成核心阶段后，可以增加一项独立集成：

- RAG 智能体通过 HTTP 工具查询 FastAPI 项目的用户、设备或工单。
- 创建工单前由 LangGraph 暂停并请求用户确认。
- FastAPI 负责权限、幂等和数据落库，Agent 不直接访问数据库。

该集成用于体现服务边界和真实工具调用，但不是两个项目独立完成的前置条件。

## 12. 允许修改的关键决策

- 业务主题可以改为任务管理、预约系统、库存系统或内容平台。
- 后台任务可以改用 Dramatiq、RQ 或 Arq。
- 对象存储可以选择 MinIO、S3 兼容服务或只保留本地适配器。
- 多租户可根据学习目标改成单租户，但会减少数据隔离和权限设计亮点。

修改原则：优先保留认证授权、事务、并发、测试和部署等后端核心能力，不为了增加页面数量而堆砌重复 CRUD。
