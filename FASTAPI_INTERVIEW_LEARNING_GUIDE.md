# FastAPI 多租户工单系统：学习重点与面试标准答案

> 适用项目：`FastAPI_web`  
> 文档定位：开发学习地图、项目讲解稿、面试题库  
> 基准日期：2026-09-13

## 1. 先给结论

这个项目真正有面试价值的部分，不是“用了 FastAPI、PostgreSQL、Redis、Celery”这些技术名称，而是你能否解释以下问题：

1. 一次请求如何从 HTTP 入口经过认证、租户识别、权限检查、业务规则、事务和数据库，再返回统一响应。
2. 如何防止跨租户越权、重复创建、并发领取、非法状态跳转和消息丢失。
3. 为什么采用当前方案，它解决了什么问题，还有哪些限制。
4. 你如何用自动化测试、迁移检查、Docker 健康检查和实际运行结果证明功能可靠。

建议把知识掌握程度理解为：

| 等级 | 面试标准 | 开发标准 |
|---|---|---|
| 精通 | 不看资料也能讲清设计、失败场景、取舍和证据 | 能独立修改、调试，并为修改补测试 |
| 熟悉 | 能结合本项目解释用途和主要实现 | 能定位代码，在指导下安全修改 |
| 了解 | 知道它是什么、为何使用、与相邻组件的关系 | 会配置、运行和排查常见问题 |
| 暂时不用管 | 主动说明不在项目范围内即可 | 当前投入产出比低，不影响项目完整性 |

## 2. 应该精通的内容

### 2.1 项目业务与完整请求链路

必须能从业务问题讲到代码，而不是从技术名词开始背诵。

完整链路：

```text
客户端
  -> Uvicorn / ASGI
  -> FastAPI 中间件（Correlation ID、耗时、日志、指标）
  -> Router（路径、请求 Schema、HTTP 语义）
  -> Dependency（Session、当前用户、当前租户、权限）
  -> Service（业务规则、事务边界）
  -> Repository（带 tenant_id 的数据访问）
  -> SQLAlchemy AsyncSession
  -> PostgreSQL
  -> 统一异常响应 / Pydantic 响应模型
```

涉及后台任务时，主链路变为：

```text
API 事务写入 BackgroundJob + OutboxMessage
  -> Celery Beat 周期扫描 Outbox
  -> Redis Broker
  -> Celery Worker 领取任务
  -> 执行、重试或记录失败
  -> PostgreSQL 保存最终任务状态
```

你需要做到：能任选一个接口，例如“领取工单”或“提交导出任务”，从入口讲到数据库提交。

### 2.2 多租户隔离

这是项目最重要的业务安全边界。

当前实现采用多层防护：

- 请求通过 `X-Tenant-ID` 明确当前租户。
- `get_current_tenant` 验证当前用户在该租户中拥有有效成员关系。
- 路径中的 `tenant_id` 必须与当前租户一致。
- Repository 查询始终包含 `tenant_id` 条件。
- 复合外键约束保证客户、创建者、负责人和工单属于同一租户。
- 跨租户资源通常返回 404，避免泄露资源是否存在。
- 集成测试覆盖已知其他租户资源 ID 仍无法访问的场景。

必须同时知道限制：当前隔离主要由应用层、Repository 和数据库约束保证，没有使用 PostgreSQL Row-Level Security。新增查询时如果漏写 `tenant_id`，仍可能产生风险，因此代码审查和跨租户测试不可缺少。

### 2.3 认证、Refresh Token 轮换与 RBAC 授权

必须区分：

- 认证 Authentication：确认“你是谁”。
- 授权 Authorization：确认“你能做什么”。
- 租户上下文：确认“你当前在哪个企业空间操作”。
- 数据范围：确认“你能操作该租户中的哪些记录”。

本项目使用密码安全哈希、JWT Access Token、Refresh Token 哈希落库、轮换、注销和重用检测。RBAC 采用 `User -> Membership -> Role -> Permission`，权限判断后还会检查工单创建者或负责人等数据范围。

### 2.4 事务、一致性与业务不变量

Service 层拥有事务边界，Repository 只负责查询和持久化操作。必须能解释：

- 为什么“工单状态变化”和 `TicketEvent` 必须在同一事务提交。
- 为什么“后台任务记录”和 `OutboxMessage` 必须在同一事务提交。
- 为什么附件文件写入成功但数据库提交失败时，要删除已经写入的文件。
- 为什么遇到 `IntegrityError` 后必须先 `rollback()` 才能继续使用 Session。

### 2.5 状态机

当前合法流转为：

```text
open --claim--> in_progress
in_progress --transfer--> in_progress
in_progress --resolve--> resolved
resolved --close--> closed
resolved --reopen--> open
closed --reopen--> open
```

普通 PATCH 只允许修改有限字段，不能绕过动作接口直接改变状态。合法动作会同时记录审计事件，非法组合返回冲突。

### 2.6 并发控制与幂等

这是区别于普通 CRUD 项目的核心亮点。

- 并发领取：查询工单时使用 `SELECT ... FOR UPDATE` 锁住同一行，后到事务等待；第一个事务提交后，第二个事务读到已分配状态并返回冲突，因此只能有一个领取成功。
- 创建幂等：以 `(tenant_id, membership_id, Idempotency-Key)` 建唯一约束，同时保存规范化请求的 SHA-256。
- 相同 Key、相同请求：返回原有工单，并标记为 replay。
- 相同 Key、不同请求：返回 409 幂等冲突。
- 两个请求同时首次写入：数据库唯一约束裁决，失败方回滚后读取胜出记录。

### 2.7 测试证据与诚实边界

面试时不能只说“我测试过”，要说测试了什么、如何隔离以及证明了什么。

当前测试覆盖：

- 单元测试：状态机、游标、安全、缓存键、日志脱敏、文件存储、配置约束。
- API 契约测试：核心路径、安全方案、响应模型和统一错误结构。
- PostgreSQL/Redis 集成测试：认证、租户隔离、RBAC、事务、分页、幂等、并发领取、附件、后台任务、查询计划和指标。
- 测试资源保护：数据库名必须以 `_test` 结尾，Redis 使用独立 DB，条件不满足时拒绝清理。
- 最近一次本地完整验收记录为 `79 passed`，但 GitHub 云端 CI 尚未实际执行。

必须诚实说明：性能阶段尚未完成，因此不能声称“高并发”“生产级”“P95 小于多少毫秒”或虚构用户规模。

## 3. 应该熟悉的内容

| 内容 | 需要达到的程度 | 项目位置 |
|---|---|---|
| FastAPI App Factory 与 lifespan | 会解释创建应用、注入替身、关闭连接池和 Redis 连接 | `src/ticketing/main.py` |
| Dependency Injection | 会解释 Session、用户、租户和权限如何逐层依赖 | `src/ticketing/api/dependencies.py` |
| SQLAlchemy AsyncSession | 会解释 Engine、连接池、每请求 Session、commit/rollback | `src/ticketing/db/` |
| Alembic | 会升级、回滚、检查迁移漂移，理解迁移不是建表脚本复制 | `alembic/` |
| Repository / Service 分层 | 知道查询与业务规则、事务边界为什么分开 | `repositories/`、`services/` |
| PostgreSQL 索引与游标分页 | 会解释复合索引顺序、稳定排序和查询计划 | `models/ticket.py`、`repositories/ticket.py` |
| Redis Cache Aside | 会解释键隔离、TTL、抖动、失效和降级 | `cache/`、`services/rbac.py` |
| 原子限流 | 会解释 Redis Lua 为什么避免 INCR/EXPIRE 竞态 | `cache/redis.py` |
| Celery 与 Outbox | 会解释 API、Beat、Broker、Worker、任务表的职责 | `tasks/`、`services/background_job.py` |
| 文件安全 | 会解释流式大小限制、签名检查、随机对象键和路径限制 | `services/ticket_collaboration.py`、`storage/local.py` |
| 可观测性 | 会解释 Correlation ID、JSON 日志、脱敏、Prometheus 指标 | `core/context.py`、`core/logging.py`、`core/observability.py` |
| Docker Compose | 会启动、停止、查看健康状态和日志，理解服务依赖 | `Dockerfile`、`compose.yml`、`compose.prod.yml` |
| CI | 会解释每个检查拦截什么风险 | `.github/workflows/ci.yml` |

## 4. 需要了解的内容

这些内容不要求你现在从零实现，但要能说出用途：

- ASGI、Uvicorn 和多 Worker 的基本关系。
- Pydantic v2 请求校验、响应模型与 `BaseSettings`。
- HTTP 方法、幂等语义以及 401、403、404、409、422、429、503 的区别。
- PostgreSQL MVCC、默认隔离级别、行锁和连接池的基本概念。
- Redis 的内存数据特性、TTL、持久化不是本项目业务真相来源。
- Celery 的至少一次投递语义、晚确认、可见性超时和预取数量。
- Prometheus Counter、Histogram、Gauge 的用途。
- Docker 镜像、容器、Volume、容器网络和端口映射。
- `uv.lock`、锁定依赖和可复现构建。
- OpenAPI 的用途，以及契约测试为何能发现意外 API 变更。
- SMTP 只是邮件传输方式；项目默认 console backend 是模拟发送。

## 5. 当前阶段暂时不用深入的内容

“不用管”不是永远不学，而是当前项目和初中级 Python 后端面试中优先级较低：

- FastAPI、Starlette、SQLAlchemy、asyncpg 的框架源码细节。
- 自己实现密码哈希算法、JWT 算法、数据库驱动或 Redis 协议。
- Kubernetes、Service Mesh、复杂云原生编排。
- Kafka、事件溯源、CQRS、Saga；当前 Outbox + Celery 已足够表达一致性问题。
- 微服务拆分；当前模块化单体更符合项目规模。
- PostgreSQL 内核、WAL 二进制格式、复杂复制与分片算法。
- Redis Cluster、Sentinel 的完整运维细节。
- OAuth2/OIDC 社交登录；项目当前目标是掌握本地认证闭环。
- 前端框架和页面美化；这是后端项目。
- 尚未实施的 MinIO、S3、分布式追踪和云部署细节。
- 没有真实测量数据时的性能数字和容量承诺。

如果面试岗位明确要求 Kubernetes、Kafka、云服务或 OAuth2，再将对应内容提升到“熟悉”。

## 6. 项目介绍标准答案

### 6.1 30 秒版本

> 我实现了一个基于 FastAPI 的多租户售后工单协作后端。系统使用 SQLAlchemy Async 和 PostgreSQL 保存用户、租户、权限、客户与工单，通过租户上下文和 RBAC 防止越权；工单流转由状态机约束，并通过行锁和幂等键处理并发领取与重复提交。Redis 用于权限缓存和登录限流，Celery 配合事务 Outbox 处理通知和导出任务。项目还有统一错误、结构化日志、Prometheus 指标、分层测试、Alembic 迁移和 Docker Compose 交付。

### 6.2 90 秒版本

> 这个项目解决多个企业共享同一套工单系统时的数据隔离和协作问题。一个用户可以属于多个租户，但每个请求都要携带当前租户，系统先验证 JWT，再验证成员关系和 RBAC 权限，Repository 查询也必须携带 tenant_id，并通过复合外键进一步避免跨租户关联。
>
> 核心业务不是简单 CRUD。工单只能通过领取、转派、解决、重开和关闭这些动作流转，状态变化和审计事件在同一事务提交。两个客服同时领取时使用 PostgreSQL 行锁串行化竞争；创建接口使用租户和成员范围内的 Idempotency-Key 加请求哈希，既避免重复资源，也能识别同一个 Key 被用于不同请求。
>
> 对于邮件和导出，API 不直接执行耗时操作，而是在同一数据库事务写入任务和 Outbox，由 Beat 发布到 Redis Broker，Worker 使用租约、重试上限和幂等执行处理。项目还实现了 Redis 缓存降级、登录限流、附件安全、统一错误、Correlation ID、JSON 日志、Prometheus 指标以及 PostgreSQL/Redis 集成测试。当前已完成本地 Docker 验收，但云端 CI 和正式性能指标仍需在发布后验证，所以我不会把它描述成已经验证的生产级系统。

## 7. 面试问题与标准答案

### 7.1 项目与架构

#### Q1：这个项目解决什么问题？

**标准答案：**

它是一个多租户售后工单协作后端。多个企业共享一套部署，但租户数据必须隔离；租户内部不同成员具有不同角色和权限；工单还要支持状态流转、并发领取、评论、附件、审计事件和异步通知。因此项目重点是安全边界和一致性，而不是堆叠 CRUD 页面。

#### Q2：为什么选择工单系统作为练习项目？

**标准答案：**

工单系统包含真实后端常见问题：认证授权、多租户隔离、状态机、事务、并发、幂等、分页索引、文件上传、缓存、异步任务和可观测性。它能展示业务规则和工程取舍，比只有用户增删改查的项目更容易证明后端能力。

#### Q3：为什么选择模块化单体，而不是微服务？

**标准答案：**

当前规模下，模块化单体可以保持一次部署和本地事务，同时通过 Router、Service、Repository 等边界控制耦合。直接拆微服务会引入服务发现、分布式事务、网络失败和运维成本，却没有真实规模证据支持。以后如果通知或导出需要独立扩缩容，已经存在的异步任务边界可以作为拆分点。

#### Q4：项目为什么分 Router、Service、Repository？

**标准答案：**

Router 负责 HTTP 协议和 Schema；Service 负责业务规则、权限相关的数据范围和事务；Repository 负责带租户条件的数据库访问。这样业务规则不会散落在路由中，Repository 也不会决定 HTTP 状态码。单元测试可以聚焦纯规则，集成测试验证事务与真实数据库行为。

#### Q5：一次创建工单请求如何执行？

**标准答案：**

请求先经过中间件生成 Correlation ID，然后 Pydantic 校验请求；依赖链验证 Access Token、解析 `X-Tenant-ID` 和活动成员关系；Service 计算请求哈希并检查幂等记录，验证客户属于当前租户且调用者有访问权限，然后在一个事务中写入 Ticket、创建事件和幂等键。成功后返回 201；重复同请求返回原资源；同 Key 不同内容返回 409。

#### Q6：为什么使用依赖倒置和 Protocol？

**标准答案：**

应用只依赖 `DatabaseProtocol`、`CacheBackend`、`AttachmentStorage`、`EmailSender` 等窄接口，而不要求调用方知道具体实现。这样测试可以注入替身，本地文件存储以后也能替换为对象存储。Protocol 的价值不是为了层数，而是明确组件契约和替换边界。

### 7.2 FastAPI、ASGI 与异步

#### Q7：FastAPI 的依赖注入在项目中做了什么？

**标准答案：**

它负责构建请求级依赖链：Session -> AuthService -> 当前用户 -> 当前租户 -> 权限检查 -> 业务 Service。路由只声明需要什么，不手动到全局变量中查找资源。测试还可以覆盖依赖，从而隔离真实数据库或模拟失败。

#### Q8：为什么使用 App Factory？

**标准答案：**

`create_app()` 可以接收 Settings、Database 和 Cache 的替代实现，避免测试导入时绑定唯一全局环境。它还把中间件、异常处理器、路由和生命周期配置集中在一个可重复构造的位置。模块末尾的 `app = create_app()` 则满足 Uvicorn 的启动入口要求。

#### Q9：lifespan 的作用是什么？

**标准答案：**

lifespan 管理应用级资源生命周期。项目在运行阶段设置 ready 状态，关闭时依次关闭 Redis 客户端和数据库 Engine，避免连接泄漏。它比散落的 startup/shutdown 钩子更容易推理和测试。

#### Q10：用了 `async def` 就一定更快吗？

**标准答案：**

不一定。异步主要提高大量 I/O 等待场景下的并发利用率，并不会让单条 SQL 或 CPU 计算变快。异步函数中如果执行阻塞 I/O，仍会阻塞事件循环；项目对同步邮件发送和文件操作使用 `asyncio.to_thread`，数据库则使用 asyncpg 和 AsyncSession。

#### Q11：AsyncSession 可以被多个并发任务共享吗？

**标准答案：**

不应共享。Session 表示一个工作单元并维护事务与对象状态，不是并发安全容器。本项目每个请求创建独立 Session，Worker 每次任务也创建独立 Session；Engine 和连接池可以是应用级共享资源。

#### Q12：Pydantic Schema 和 SQLAlchemy Model 有什么区别？

**标准答案：**

Schema 是 API 边界的数据合同，负责输入校验和稳定输出；ORM Model 表示数据库持久化结构、约束和关系。两者分离可以避免把密码哈希、内部对象键等字段意外返回，也避免数据库结构直接绑死外部 API。

#### Q13：为什么统一错误响应？

**标准答案：**

客户端需要依赖稳定的错误码，而不是解析变化的文字。项目把 HTTP 异常、请求校验错误和未知异常统一为 `detail.code`、`detail.message` 和 `correlation_id`；500 不返回内部堆栈，详细异常只写入脱敏日志。

### 7.3 PostgreSQL、事务与多租户

#### Q14：Engine、连接、Session 和事务分别是什么？

**标准答案：**

Engine 管理数据库方言和连接池；连接是一次实际数据库连接；Session 是 ORM 工作单元；事务决定一组读写是整体提交还是整体回滚。项目共享 Engine，每个请求使用独立 Session，Service 明确调用 commit 或 rollback。

#### Q15：为什么事务边界放在 Service，而不是 Repository？

**标准答案：**

一个业务动作常跨多个 Repository。例如状态变更同时要更新 Ticket 并写 TicketEvent；后台任务提交同时写 BackgroundJob 和 OutboxMessage。只有 Service 看得到完整业务动作，若每个 Repository 自己提交，就无法保证整体原子性。

#### Q16：flush 和 commit 有什么区别？

**标准答案：**

flush 把当前变更发送到数据库，使数据库约束被检查并获得后续操作需要的数据，但事务仍可回滚；commit 才使事务持久化。创建租户时先 flush Tenant，再创建 Membership 和默认 RBAC，最后一次 commit，保证整个初始化过程原子完成。

#### Q17：为什么发生 IntegrityError 后要 rollback？

**标准答案：**

数据库语句失败后，当前事务处于失败状态，Session 不能直接继续正常查询。先 rollback 才能清理事务状态。项目在并发幂等唯一冲突后回滚，再查询胜出的幂等记录并返回一致结果。

#### Q18：项目如何建立当前租户上下文？

**标准答案：**

客户端发送 Access Token 和 `X-Tenant-ID`。系统先识别用户，再查询该用户与租户的 Membership，要求状态为 active，并确认租户仍有效。之后所有业务 Service 都接收 `CurrentTenant`，路径租户 ID 也必须与它一致。

#### Q19：项目如何防止水平越权？

**标准答案：**

不能只检查用户已登录。项目同时验证租户成员关系、Repository 的 `tenant_id` 条件、记录级数据范围和复合外键。即使攻击者猜到其他租户的 UUID，查询也不会在当前租户范围内命中，并返回 404，集成测试专门覆盖这种情况。

#### Q20：为什么跨租户资源常返回 404，而不是 403？

**标准答案：**

403 会间接确认资源存在。对于当前租户范围外的资源，统一表现为“找不到”可以减少 ID 枚举造成的信息泄露。对已确认处于当前租户内、但功能权限不足的操作，才使用 403。

#### Q21：复合外键在多租户系统中有什么价值？

**标准答案：**

只使用单独 UUID 外键只能证明目标存在，不能证明它和当前记录属于同一租户。`(tenant_id, customer_id)` 指向 `customers(tenant_id, id)` 这类复合外键把租户一致性下沉到数据库，即使应用代码出现错误，也不能把 A 租户工单关联到 B 租户客户。

#### Q22：为什么没有只依赖数据库 RLS？

**标准答案：**

当前项目以应用层显式条件、复合约束和测试为主，便于学习和调试；没有声称使用了 RLS。更高安全要求下可以增加 RLS 作为纵深防御，但还需可靠地把租户上下文绑定到每个连接和事务，否则配置错误同样危险。

#### Q23：为什么使用游标分页？

**标准答案：**

大偏移量分页需要数据库跳过大量行，而且数据插入时容易重复或漏项。项目按 `(created_at DESC, id DESC)` 稳定排序，游标保存最后一条记录的两个字段，下一页使用小于条件继续查询。`id` 是相同创建时间下的稳定决胜字段。

#### Q24：为什么为工单建立多个复合索引？

**标准答案：**

索引围绕真实查询模式设计，租户 ID 放在前面，然后是状态、负责人、优先级或创建者，最后连接分页排序字段 `created_at` 和 `id`。这样数据库可先缩小租户和筛选范围，再按分页顺序扫描。项目用 `EXPLAIN` 集成测试验证状态筛选查询确实使用目标索引。

#### Q25：索引越多越好吗？

**标准答案：**

不是。索引占空间，会增加 INSERT、UPDATE 和维护成本，也可能因选择性差而不被优化器采用。应该从真实查询和查询计划出发，而不是为每个字段机械建索引。

### 7.4 认证、授权与安全

#### Q26：密码为什么不能加密后保存？

**标准答案：**

密码验证不需要恢复原文，应使用带盐、抗暴力破解的单向密码哈希。项目使用 pwdlib 推荐算法；数据库只保存哈希。加密需要可解密密钥，一旦密钥泄露就会批量暴露密码。

#### Q27：项目为什么同时使用 Access Token 和 Refresh Token？

**标准答案：**

Access Token 生命周期短，用于频繁访问 API；Refresh Token 生命周期长，只用于换取新令牌。这样减少长期凭据在每次请求中暴露的风险，同时避免用户频繁登录。两者通过 `type` Claim 强制区分，Refresh Token 不能当 Access Token 使用。

#### Q28：JWT 中有哪些关键 Claim？

**标准答案：**

项目要求 `sub`、`jti`、`type`、`iat` 和 `exp`。`sub` 是用户 ID，`jti` 是令牌唯一 ID，`type` 区分 access/refresh，`iat` 和 `exp` 表示签发与过期时间。签名算法当前是 HS256，生产环境必须使用足够强且不入库的 secret。

#### Q29：为什么 Refresh Token 数据库只保存哈希？

**标准答案：**

Refresh Token 是长期凭据。如果数据库泄露，明文 Token 可以直接用于换取新令牌；保存 SHA-256 哈希后，服务仍能比较客户端 Token 是否匹配，但数据库内容不能直接作为凭据使用。这与保存密码哈希的目标类似，但威胁模型和算法选择不同。

#### Q30：Refresh Token 轮换如何工作？

**标准答案：**

刷新时按 `jti` 加行锁读取记录，验证用户、哈希、有效期和撤销状态，然后签发新 Token，将旧 Token 标记撤销并记录 replacement。若被替换的旧 Token 再次出现，系统认为可能发生重放，撤销该用户所有活动 Refresh Token。

#### Q31：注销后 Access Token 是否立即失效？

**标准答案：**

当前不会。注销撤销 Refresh Token，已经签发的 Access Token 仍可使用到短 TTL 结束；修改密码也撤销活动 Refresh Token。若业务要求立即踢下线，可以增加 Access Token denylist、用户 token version 或服务端会话，但会增加每次请求的状态检查成本。

#### Q32：如何降低登录接口的账号枚举风险？

**标准答案：**

账号不存在和密码错误都返回统一的“凭据无效”，不告诉客户端邮箱是否已注册。账号不存在时仍执行一次 dummy password verify，使计算路径更接近真实账号，减少明显时序差异；同时使用按客户端地址和规范化邮箱组合的限流键。

#### Q33：RBAC 数据模型是什么？

**标准答案：**

User 通过 Membership 加入 Tenant；Membership 可以关联多个 Role；Role 再关联多个 Permission。默认角色有 tenant_admin、agent 和 requester。权限是稳定代码，例如 `tickets.manage.workflow`，业务代码检查权限代码而不是到处判断角色名称。

#### Q34：为什么权限和角色要分开？

**标准答案：**

角色是权限集合。若业务代码直接判断角色名，新增角色或调整职责需要修改大量代码；检查权限代码后，可以在数据层重新组合角色。它也更符合最小权限原则。

#### Q35：功能权限和数据范围权限有什么区别？

**标准答案：**

功能权限决定能否执行一类动作，例如能否管理工单流程；数据范围决定能操作哪些记录，例如 requester 通常只能看到自己创建的工单。项目先检查权限，再根据工单创建者、负责人等关系限制数据范围，不能把二者混为一谈。

#### Q36：401、403 和 404 如何选择？

**标准答案：**

未提供或提供无效身份凭据返回 401；身份有效但缺少当前租户内的操作权限返回 403；资源在当前租户或当前数据范围中不可见时返回 404，以避免泄露其他租户资源。选择依据是认证状态和资源可见边界。

### 7.5 状态机、并发与幂等

#### Q37：为什么不能通过 PATCH 直接修改工单状态？

**标准答案：**

状态变化不仅是字段更新，还包含允许的前置状态、操作者权限、负责人规则和审计事件。若普通 PATCH 能改 status，就可以绕过这些不变量。项目把状态流转暴露为 claim、transfer、resolve、reopen、close 等动作接口，PATCH 只修改有限普通字段。

#### Q38：状态机比一堆 if/else 好在哪里？

**标准答案：**

显式转换表把“动作 + 当前状态 -> 下一状态”集中定义，容易审查全部合法路径，也能系统测试所有未定义组合都被拒绝。分散的 if/else 容易出现不同接口规则不一致。

#### Q39：审计事件和应用日志有什么区别？

**标准答案：**

TicketEvent 是业务事实，记录谁对哪张工单做了什么，应与工单变更同事务持久化并支持业务查询；应用日志用于运行排错，可能轮转、聚合或丢弃。不能用普通日志代替业务审计记录。

#### Q40：两个客服同时领取同一工单会发生什么？

**标准答案：**

两个事务都按租户和工单 ID 查询，但使用 `FOR UPDATE`。数据库让同一行上的竞争串行化。第一个事务将负责人和状态提交后，第二个事务继续并看到最新状态或已有负责人，于是返回 409。集成测试验证并发请求只有一个成功。

#### Q41：为什么选择悲观行锁，而不是只在 Python 中加锁？

**标准答案：**

Python 进程锁只保护单进程，部署多个 Uvicorn Worker 或多个容器后无效。PostgreSQL 行锁由共享数据库协调所有实例，且锁范围只覆盖竞争工单。代价是竞争时等待，因此事务必须短，并保持一致的加锁顺序。

#### Q42：什么是幂等？

**标准答案：**

幂等表示同一个逻辑请求重复执行不会产生多个业务效果。网络超时后客户端不知道服务端是否成功，重试很常见；项目通过 Idempotency-Key 把重复创建绑定到原结果，而不是简单拒绝所有重复请求。

#### Q43：为什么幂等键还要保存请求哈希？

**标准答案：**

只保存 Key 无法判断调用方是否错误地把同一个 Key 用于不同内容。项目对规范化请求生成 SHA-256：相同 Key、相同哈希返回原结果；相同 Key、不同哈希返回 409。这样避免静默返回与当前请求不一致的资源。

#### Q44：只在业务代码中先查幂等键是否存在，够不够？

**标准答案：**

不够。两个并发事务可能都先查到不存在，然后同时插入。数据库唯一约束才是最终裁决；业务层捕获 `IntegrityError`、rollback，再读取获胜记录。正确方案是“快速预查 + 唯一约束兜底”。

#### Q45：行锁、乐观锁和条件更新如何选择？

**标准答案：**

行锁适合竞争集中、逻辑需要读取后再修改的短事务；乐观锁适合冲突较少的场景，通过版本号发现丢失更新；条件更新可用一条 `UPDATE ... WHERE status=...` 高效竞争。本项目为清晰表达领取流程选择行锁，但高吞吐热点场景可以评估条件更新。

### 7.6 Redis、后台任务与最终一致性

#### Q46：Redis 在项目中承担什么职责？

**标准答案：**

一是缓存成员权限快照，减少重复权限查询；二是用 Lua 实现登录固定窗口限流；三是作为 Celery Broker。PostgreSQL 才是业务真相来源，Redis 缓存丢失不能导致租户数据错误。

#### Q47：权限缓存如何避免跨租户污染？

**标准答案：**

缓存键包含版本前缀、tenant_id 和 membership_id，权限内容不会只按 user_id 共享。角色变化后删除对应成员缓存，TTL 提供最终兜底，随机抖动减少大量键同时过期造成的压力。

#### Q48：Redis 不可用时系统如何处理？

**标准答案：**

普通缓存操作 fail-open：读取失败按未命中处理，回源 PostgreSQL；写入或失效失败记录指标和警告，但核心授权仍从数据库计算。登录限流也按可用性优先返回 degraded/allowed，这意味着 Redis 故障时限流保护减弱，是必须明确的安全取舍。

#### Q49：为什么限流使用 Lua 脚本？

**标准答案：**

固定窗口需要 `INCR`，并在第一次计数时设置 `EXPIRE`。若分成多个网络命令，进程崩溃或并发交错可能留下没有 TTL 的键。Lua 在 Redis 内原子执行计数与过期设置，并返回当前计数和剩余 TTL。

#### Q50：什么是缓存穿透、击穿和雪崩？项目如何应对？

**标准答案：**

穿透是大量查询不存在数据绕过缓存；击穿是热点键失效时大量请求同时回源；雪崩是大量键同时过期。项目目前通过受控键空间、TTL 和 TTL 抖动降低部分风险，并允许回源数据库；没有实现布隆过滤器或互斥重建，不应声称完全解决这些问题。

#### Q51：为什么需要后台任务？

**标准答案：**

邮件和批量导出耗时且可能依赖外部服务，不应让 HTTP 请求一直等待。API 只校验并记录任务，返回 202 和任务 ID；Worker 独立执行，客户端查询任务状态。这样可以重试、限时并独立调整 Worker 并发。

#### Q52：直接在数据库 commit 后调用 Celery 有什么问题？

**标准答案：**

数据库提交成功后进程可能在发送消息前崩溃，导致任务永久丢失；如果先发消息再提交，Worker 又可能读不到业务数据。项目把 BackgroundJob 和 OutboxMessage 放在同一数据库事务，之后由 Dispatcher 重试发布，缩小双写不一致窗口。

#### Q53：Outbox 是否保证 exactly-once？

**标准答案：**

不能。发布成功但更新 Outbox 状态前崩溃时，消息可能再次发布；Broker 和 Worker 也可能重复投递。系统目标是至少一次投递，并要求消费者幂等。任务 ID、数据库状态、行锁和终态检查让成功或失败终态的重复执行成为 no-op。

#### Q54：后台任务的租约有什么作用？

**标准答案：**

Worker 领取任务时把状态设为 running 并写 `lease_expires_at`。租约有效时其他 Worker 不重复执行；Worker 崩溃后租约到期，任务可被恢复。任务还有 attempt_count、max_attempts、指数退避和软硬超时，防止无限快速重试。

#### Q55：为什么邮件出现“不确定是否发送成功”时不自动重发？

**标准答案：**

外部邮件服务器可能已接收邮件，但 Worker 在记录成功前崩溃。此时自动重发可能导致用户收到重复邮件。项目把这种状态标记为 ambiguous failure 并抑制自动重发，交给人工或可查询提供商确认。这是在“可能漏发”和“避免重复发送”之间的明确取舍。

#### Q56：Celery 的 `acks_late` 和 `worker_prefetch_multiplier=1` 有什么作用？

**标准答案：**

晚确认让任务在执行完成后才确认，Worker 中途死亡时 Broker 可以重新投递；预取 1 减少一个 Worker 提前占有大量长任务。它们提高恢复能力，但也增加重复执行可能，因此不能替代幂等消费者。

### 7.7 文件、安全、可观测性与测试

#### Q57：附件上传做了哪些安全处理？

**标准答案：**

系统先验证工单访问范围；只允许有限扩展名和 MIME 对应关系；检查 PNG、JPEG、PDF 文件签名，文本要求合法 UTF-8 且拒绝 NUL；流式累计大小并限制上限；存储使用随机对象键而不是用户文件名；解析后确认路径仍在存储根目录内，并用独占创建避免覆盖。数据库提交失败时删除已写文件。

#### Q58：这些检查是否足以替代杀毒和内容扫描？

**标准答案：**

不足。魔数和 MIME 只能拦截明显伪装，不能发现恶意 PDF、图片解析漏洞或木马。生产系统还应考虑恶意软件扫描、对象存储、隔离区、下载响应头、配额和生命周期策略。当前实现是安全基础，不应描述为完整文件安全平台。

#### Q59：为什么使用 Correlation ID？

**标准答案：**

它把同一请求的响应、日志和后台任务关联起来。系统只接受符合规则的客户端 ID，否则生成新 ID；中间件把它写回响应，提交后台任务时放入 Outbox payload，Worker 执行时恢复上下文。用户报告错误时可以用该 ID定位链路。

#### Q60：结构化日志比普通字符串日志好在哪里？

**标准答案：**

JSON 日志把时间、级别、路由、状态、耗时、错误类型和 Correlation ID 作为可查询字段，便于日志平台过滤和聚合。项目 Formatter 会按字段名和文本模式脱敏 Token、密码、邮箱、手机号和附件信息，避免排错日志泄露敏感数据。

#### Q61：项目暴露了哪些指标？

**标准答案：**

包括 HTTP 请求数、请求耗时、错误类型、数据库查询耗时、后台任务各状态数量和成功率，以及 Redis 缓存命中、失败和限流计数。HTTP 指标使用路由模板而不是原始路径，避免把 UUID 变成高基数标签。

#### Q62：当前 Prometheus 指标有什么限制？

**标准答案：**

指标 Registry 在进程内创建。多个 Uvicorn Worker 各自维护一份内存指标，请求打到哪个进程就看到哪一份，不能天然得到全局准确聚合。生产环境需要 Prometheus Python multiprocess 模式、单独指标进程或外部遥测方案。

#### Q63：liveness 和 readiness 有什么区别？

**标准答案：**

liveness 表示进程还能响应 HTTP，失败通常意味着应重启；readiness 表示实例当前能否接收流量。项目 readiness 检查生命周期状态和 PostgreSQL `SELECT 1`，数据库不可用返回 503。Redis 有独立 degraded 健康接口，因为缓存故障按策略不一定阻断核心 API。

#### Q64：测试为什么必须使用独立 PostgreSQL 和 Redis？

**标准答案：**

集成测试会 TRUNCATE 表和 FLUSHDB，如果连接到开发库会破坏真实数据。项目要求测试数据库名以 `_test` 结尾，Redis 使用独立 DB 15，Broker 使用独立 DB 14；保护条件不满足就 fail-closed，拒绝执行清理。

#### Q65：单元测试、API 测试和集成测试分别证明什么？

**标准答案：**

单元测试快速验证纯规则和边界输入；API 契约测试验证路由、安全声明、响应模型和错误格式；集成测试使用真实 PostgreSQL/Redis 验证 SQL、锁、约束、事务、缓存和并发行为。三者互补，Mock 测试不能证明数据库锁或查询计划真的有效。

#### Q66：如何保证测试互不污染？

**标准答案：**

集成测试前后清理独立数据库、Redis 和测试文件目录；Repository 级测试还可使用外层事务并在 fixture 结束后回滚。测试不能依赖执行顺序或上一个测试留下的数据。

#### Q67：OpenAPI 契约测试的价值是什么？

**标准答案：**

它会固定关键路径、认证要求、Header、成功响应模型和错误响应格式。重构时如果意外删除鉴权、改错状态码或让响应 Schema 漂移，CI 能在客户端受影响前发现。

### 7.8 Docker、CI、性能与交付

#### Q68：Dockerfile 为什么使用多阶段构建？

**标准答案：**

builder 阶段使用 uv 根据 lock 文件安装生产依赖和项目；runtime 阶段只复制虚拟环境、迁移文件和必要运行内容。这样运行镜像不需要完整构建工具，职责更清晰，并通过 `--frozen` 防止构建时依赖漂移。

#### Q69：为什么容器使用非 root 用户？

**标准答案：**

如果应用或依赖被利用，非 root 可以降低攻击者在容器内的权限。项目创建固定 UID/GID 10001 的 ticketing 用户，并只给附件和导出目录所需权限。它是纵深防御，不代表容器已经绝对安全。

#### Q70：Compose 中 migrate 为什么是独立一次性服务？

**标准答案：**

迁移必须在 API 和 Worker 使用新结构前完成。migrate 等 PostgreSQL 健康后执行 `alembic upgrade head`，成功退出后 API、Worker 和 Beat 才启动。这样比每个 Web Worker 同时尝试迁移更可控。

#### Q71：开发和生产 Compose 有什么差异？

**标准答案：**

开发配置提供本机端口和安全的开发默认值；生产覆盖要求显式数据库凭据和至少 32 字符的非占位 secret，PostgreSQL 和 Redis 不发布宿主端口，并使用生产环境标识和正式 Worker 参数。当前 API 仍需由受信任反向代理或平台提供 TLS 和外部入口。

#### Q72：为什么本机端口只绑定 `127.0.0.1`？

**标准答案：**

绑定 `0.0.0.0` 会让同一网络中的其他设备也可能访问开发数据库、Redis 或 API。绑定回环地址只允许本机连接，降低开发环境暴露面；生产覆盖进一步取消数据库和 Redis 的宿主端口。

#### Q73：CI 每一步分别防止什么问题？

**标准答案：**

锁定依赖安装验证环境可复现；Ruff 发现风格和常见代码问题；strict mypy 检查类型边界；pytest 验证业务和真实依赖行为；`alembic check` 发现模型与迁移漂移；Docker build 验证交付镜像能构建。PostgreSQL 和 Redis service 让集成测试不依赖开发者机器已有状态。

#### Q74：现在能说项目已通过 GitHub CI 吗？

**标准答案：**

不能。CI 文件已完成，本地等价检查和 Docker 验收已通过，但项目此前尚未推送到 GitHub，因此云端 Workflow 没有真实运行记录。推送后必须查看 Actions 结果，才能说 GitHub CI 通过。

#### Q75：现在能说项目支持高并发或达到生产级吗？

**标准答案：**

不能。项目实现了行锁、异步 I/O、连接池和多 Worker 等机制，但阶段 16 的压力测试、P50/P95、吞吐量、错误率和资源环境尚未形成可复现数据。更准确的说法是“实现并测试了关键并发正确性和容器化运行方式”。

#### Q76：这个项目当前最大的生产限制是什么？

**标准答案：**

主要限制包括：附件和导出使用本地 Volume，不适合多节点共享；默认邮件是 console 模拟；缺少真实 TLS/反向代理和密钥管理；指标是进程内的；未做备份恢复、HA、容量与压力验证；租户隔离没有 RLS；GitHub CI 尚待远端验证。这些是明确的下一阶段工作，不应隐藏。

### 7.9 开放题与追问

#### Q77：项目中最有挑战的部分是什么？

**标准答案：**

我会选择“一致性”来回答，因为它贯穿多个功能：并发领取要保证只有一个负责人；重复创建要返回同一业务结果；状态变化和审计事件必须原子提交；数据库任务与消息 Broker 之间要通过 Outbox 衔接；Worker 重试又必须避免重复副作用。我的处理方式是先明确不变量，再选择数据库约束、事务、行锁、幂等键、Outbox 和集成测试分别提供证据。

#### Q78：如果让你继续改进，优先做什么？

**标准答案：**

先完成阶段 16：建立可重复压测环境，记录 P50/P95、吞吐和错误率，并用结果调整连接池与 Worker；补 README、ER 图、演示数据和故障恢复说明。之后再处理生产差距，例如对象存储、外部密钥管理、TLS/反向代理、多进程指标和备份恢复。顺序由证据和风险驱动，而不是盲目增加技术栈。

#### Q79：如果数据量增长，最先观察什么？

**标准答案：**

先看请求延迟与错误率，再用数据库查询耗时和 `EXPLAIN ANALYZE` 找慢查询，观察连接池等待、锁等待、索引命中、Redis 命中率、任务积压和 Worker 执行时间。只有找到瓶颈后才决定加索引、改查询、扩 Worker 或引入新组件。

#### Q80：如果 Redis 完全宕机，哪些功能受影响？

**标准答案：**

权限缓存会退化为 PostgreSQL 查询，核心权限判断仍可运行；登录限流会 fail-open，安全保护减弱并产生 degraded 指标；Celery Broker 不可用会阻止 Outbox 消息即时发布，但 Outbox 保留在 PostgreSQL，Broker 恢复后可重试。健康检查会把缓存状态报告为 degraded。

#### Q81：如果 PostgreSQL 宕机，为什么 readiness 应失败？

**标准答案：**

PostgreSQL 是业务真相来源，绝大多数请求无法在没有数据库时正确处理。readiness 返回 503 可以让负载均衡器停止把流量发送到该实例；单纯进程仍活着并不代表服务可用。

#### Q82：你在这个项目中的个人贡献应该怎样回答？

**标准答案：**

只描述自己真正理解、运行和验证过的部分。推荐结构是：“我负责或参与了某个模块的规则梳理、实现验证和测试；遇到的具体问题是什么；我如何用代码、命令或测试定位；最后取得了什么可复现结果。”如果代码是在 AI 辅助下完成，要诚实说明使用了辅助工具，并强调自己能解释设计、复现验证、发现问题和做出取舍，而不是声称全部手写。

#### Q83：面试官让你现场改一个功能时应该怎么做？

**标准答案：**

先复述业务规则和失败场景，再定位 Router、Schema、Service、Repository、Model/迁移和测试中受影响的层；保持租户条件与事务边界；先补或更新测试，再做最小实现；最后运行 Ruff、mypy、相关测试和迁移检查。不要一上来只改路由。

## 8. 必须能现场画出的三张图

### 8.1 请求链路

```text
HTTP
 -> Observability Middleware
 -> Router + Pydantic
 -> CurrentUser
 -> CurrentTenant
 -> Permission / Data Scope
 -> Service Transaction
 -> Repository
 -> PostgreSQL / Redis
 -> Uniform Response
```

### 8.2 多租户 RBAC

```text
User
  -> Membership <- Tenant
       -> MembershipRole
            -> Role
                 -> RolePermission
                      -> Permission
```

### 8.3 可靠后台任务

```text
API Transaction
  ├── BackgroundJob
  └── OutboxMessage
          -> Dispatcher / Beat
          -> Redis Broker
          -> Celery Worker
          -> lease + retry + idempotent terminal check
          -> BackgroundJob result/status
```

## 9. 代码阅读顺序

不要按文件名随机阅读，按依赖顺序学习：

1. `FASTAPI_WEB_PROJECT_PLAN.md`：业务目标、阶段和边界。
2. `src/ticketing/main.py`：应用组装与生命周期。
3. `src/ticketing/api/v1/`：从外部看到的 HTTP 接口。
4. `src/ticketing/api/dependencies.py`：认证、租户和权限链。
5. `src/ticketing/schemas/`：输入输出合同。
6. `src/ticketing/services/ticket_workflow.py`：先理解纯状态规则。
7. `src/ticketing/services/ticket.py`：事务、权限、锁和幂等。
8. `src/ticketing/repositories/` 与 `models/`：查询、约束和索引。
9. `src/ticketing/cache/`：缓存键、降级和原子限流。
10. `src/ticketing/services/background_job.py` 与 `tasks/`：Outbox 和任务执行。
11. `src/ticketing/core/`：配置、错误、日志和指标。
12. `tests/`：用测试反推每项设计承诺和失败场景。
13. `Dockerfile`、Compose 与 CI：交付链路。

## 10. 学习验收清单

### 10.1 精通验收

- [ ] 不看文档完成 90 秒项目介绍。
- [ ] 画出请求链路、RBAC 模型和 Outbox 链路。
- [ ] 从代码解释一次领取工单的锁、权限、状态机、事务和错误响应。
- [ ] 从代码解释一次重复创建的预查、请求哈希、唯一约束和并发兜底。
- [ ] 解释至少五个生产限制，不使用“生产级”“高并发”等无证据表述。
- [ ] 能为一个新业务规则补单元测试和 PostgreSQL 集成测试。

### 10.2 熟悉验收

- [ ] 能启动/停止 Compose，并查看 API、Worker、Beat、PostgreSQL 和 Redis 状态。
- [ ] 能创建 Alembic 迁移、升级、回滚并运行 `alembic check`。
- [ ] 能解释权限缓存何时命中、何时失效、Redis 故障如何降级。
- [ ] 能解释 401、403、404、409、422、429 和 503 的项目使用场景。
- [ ] 能根据慢查询的过滤与排序设计候选复合索引，并用查询计划验证。
- [ ] 能追踪一个 Correlation ID 对应的 API 和后台任务日志。

### 10.3 面试前证据验收

- [ ] GitHub Actions 有真实绿色运行记录后，再说“云端 CI 通过”。
- [ ] 重新运行完整测试并记录准确通过数量。
- [ ] 完成阶段 16 压测后，再使用任何延迟、吞吐或并发容量数字。
- [ ] 简历中的每个数字都能给出命令、环境和结果。
- [ ] 准备一个失败案例：现象、排查、根因、修复和防回归测试。

## 11. 推荐回答结构

遇到任何技术追问，优先使用下面的五段式结构：

1. **问题**：业务或工程风险是什么。
2. **方案**：本项目采用什么机制。
3. **实现**：落在哪一层、关键数据或约束是什么。
4. **证据**：什么测试、命令或运行结果证明它有效。
5. **限制**：当前方案不能保证什么，下一步如何改进。

示例：

> 问题是网络重试可能重复创建工单。项目要求 `Idempotency-Key`，并按租户和成员划定作用域，同时保存规范化请求哈希。业务层先查已有记录，数据库唯一约束处理并发竞态；相同请求返回原结果，不同请求返回 409。集成测试覆盖顺序重放、负载冲突和两个并发请求只有一个业务效果。当前幂等记录还没有归档策略，数据长期增长时需要增加保留周期。

## 12. 最终学习优先级

```text
第一优先：项目链路、多租户、认证授权、事务、状态机、并发、幂等、测试证据
第二优先：FastAPI DI、Async SQLAlchemy、索引分页、Redis、Celery Outbox、可观测性
第三优先：Alembic、文件安全、Docker、CI、OpenAPI、配置管理
暂缓：微服务、Kubernetes、Kafka、复杂云架构、框架源码和无证据性能调优
```

真正的“精通”不是记住所有代码，而是能从业务不变量出发，解释当前实现为什么正确、如何验证、哪里仍可能失败，以及怎样在不破坏安全边界的情况下继续修改。
